/**
 * The active home's 2D sheet, as raw SVG — the drawing the CAD branch
 * generates, shown verbatim by the 2D tab and used as the AI plan reference.
 */

export { sheetSvg } from '../homes/active'
